import { useState } from 'react';
import '../logic';

const Main: React.FC = () => {
  const [item, setItem] = useState({});

  return (
    <>
      <p>Новая страничка</p>
    </>
  );
};

export default Main;
